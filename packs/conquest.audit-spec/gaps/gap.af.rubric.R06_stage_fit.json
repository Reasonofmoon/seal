{
  "id": "gap.af.rubric.R06_stage_fit",
  "ask": "[AF conquest R06 stage_fit] Is the proposed scope appropriate for the declared tier/stage rather than enterprise-everything?",
  "risk": "product_foundation",
  "schema": {
    "type": "object",
    "required": [
      "evidence_refs",
      "assertion"
    ],
    "properties": {
      "evidence_refs": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string"
        }
      },
      "assertion": {
        "type": "string",
        "minLength": 12
      },
      "known_issues": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    }
  },
  "requires": [
    "gap.af.spec.corpus"
  ],
  "fillers": [
    "human",
    "generative.af.spec-audit-row",
    "import"
  ],
  "af_legacy": {
    "rubric_id": "R06",
    "category": "stage_fit",
    "measures": "scope matches declared tier/stage"
  },
  "accept": [
    {
      "id": "passes_category",
      "type": "noul",
      "instructions": {
        "question": "Is the proposed scope appropriate for the declared tier/stage rather than enterprise-everything?",
        "focus": "Category R06 (stage_fit). Fail if detection patterns fire.",
        "compare": [
          "`facts.spec_text`",
          "`proposal.value`",
          "`facts.known_cli_flags`"
        ],
        "fail_examples": [
          "marketplace+DRM+LTI in vibe tier"
        ]
      },
      "criteria": {
        "true": {
          "what": "Spec satisfies stage_fit",
          "examples": [
            "Clear evidence for scope matches declared tier/stage"
          ]
        },
        "false": {
          "what": "Spec fails stage_fit",
          "examples": [
            "marketplace+DRM+LTI in vibe tier"
          ]
        }
      },
      "noul_gte": 0.72
    },
    {
      "id": "evidence_grounded",
      "type": "noul",
      "instructions": {
        "question": "Do `proposal.value.evidence_refs` quote or point to real passages that exist in `facts.spec_text`?",
        "compare": [
          "`proposal.value.evidence_refs`",
          "`facts.spec_text`"
        ]
      },
      "noul_gte": 0.7
    }
  ]
}
