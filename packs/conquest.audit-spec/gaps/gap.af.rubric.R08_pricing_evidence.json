{
  "id": "gap.af.rubric.R08_pricing_evidence",
  "ask": "[AF conquest R08 pricing_evidence] Is pricing backed by comps, interviews, or marked as assumption-to-verify?",
  "risk": "product_foundation",
  "schema": {
    "type": "object",
    "required": [
      "evidence_refs",
      "assertion"
    ],
    "properties": {
      "evidence_refs": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string"
        }
      },
      "assertion": {
        "type": "string",
        "minLength": 12
      },
      "known_issues": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    }
  },
  "requires": [
    "gap.af.spec.corpus"
  ],
  "fillers": [
    "human",
    "generative.af.spec-audit-row",
    "import"
  ],
  "af_legacy": {
    "rubric_id": "R08",
    "category": "pricing_evidence",
    "measures": "pricing grounded"
  },
  "accept": [
    {
      "id": "passes_category",
      "type": "noul",
      "instructions": {
        "question": "Is pricing backed by comps, interviews, or marked as assumption-to-verify?",
        "focus": "Category R08 (pricing_evidence). Fail if detection patterns fire.",
        "compare": [
          "`facts.spec_text`",
          "`proposal.value`",
          "`facts.known_cli_flags`"
        ],
        "fail_examples": [
          "₩ ranges labeled only as 'App Factory 추정' with no verify path"
        ]
      },
      "criteria": {
        "true": {
          "what": "Spec satisfies pricing_evidence",
          "examples": [
            "Clear evidence for pricing grounded"
          ]
        },
        "false": {
          "what": "Spec fails pricing_evidence",
          "examples": [
            "₩ ranges labeled only as 'App Factory 추정' with no verify path"
          ]
        }
      },
      "noul_gte": 0.72
    },
    {
      "id": "evidence_grounded",
      "type": "noul",
      "instructions": {
        "question": "Do `proposal.value.evidence_refs` quote or point to real passages that exist in `facts.spec_text`?",
        "compare": [
          "`proposal.value.evidence_refs`",
          "`facts.spec_text`"
        ]
      },
      "noul_gte": 0.7
    }
  ]
}
